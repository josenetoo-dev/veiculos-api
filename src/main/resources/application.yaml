spring:
  application:
    name: veiculos-api
  datasource:
    url: jdbc:mysql://localhost:3306/projeto_veiculos
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false